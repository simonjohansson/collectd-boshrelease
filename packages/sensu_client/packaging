# -------------------------------------------------------------------
# Packaging script for the sensu_client python application
#
# This packaging script does not build antything.
# It simply copies the Python files into the package.
# -------------------------------------------------------------------

set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

cp -v sensu_client/*.py ${BOSH_INSTALL_TARGET}/